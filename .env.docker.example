# Local PostgreSQL used only by integration tests. Copy to .env.docker.local.
POSTGRES_USER=finance_portal_test
POSTGRES_PASSWORD=replace-with-a-local-only-password
POSTGRES_DB=finance_portal_test
POSTGRES_PORT=55432
