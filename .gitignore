# Dependencies
node_modules/

# Next.js
.next/
out/

# Test output
coverage/
test-results/
# Trazas y capturas de fallos de Playwright: evidencia local de una corrida, no
# un artefacto del repositorio.
tests/e2e/.output/
playwright-report/

# Environment files (the example is intentionally versioned)
.env
.env.*
!.env.example
!.env.docker.example
!.env.docker.personal.example

# Logs and local tooling
*.log
.vercel/
# Agent runtime: skills and settings are versioned, per-machine overrides are not.
.claude/settings.local.json
.impeccable/config.local.json
.impeccable/direction-options.json
.impeccable/questions/
.impeccable/review/
.impeccable/mocks/
.impeccable/*-brief-body.md
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts
